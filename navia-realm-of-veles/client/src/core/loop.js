function gameLoop() {
    // Tu telefon gracza będzie przeliczał ruchy NPC na mapie
    // przed wysłaniem ich do walidacji na serwer.
    requestAnimationFrame(gameLoop);
}
gameLoop(); // Start pętli